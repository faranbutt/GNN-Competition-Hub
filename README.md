# GNN Competition Hub: LLM vs. Human Benchmark
This repository serves as a centralized archive for my Graph Neural Network (GNN) designs, best-performing model weights and prediction files across various competitive challenges.

## 🏆 Competition Overview: GNN Design Challenge
The primary focus of this work is the LLM vs. Human task, where human-engineered architectures are benchmarked against AI-generated baselines to push the state-of-the-art in graph representation learning.

